# TradingAgents Architecture Overview

## The Multi-Agent System (LangGraph)
TradingAgents operates on a robust multi-agent graph architecture designed to isolate responsibilities:
- **Researchers**: Retrieve raw data (fundamentals, options chains, crypto derivatives) from Dhan, Delta, Alpha Vantage, and YFinance.
- **Analysts**: Synthesize the retrieved data into cohesive outlooks (Market Analyst, Fundamental Analyst).
- **Debators (Risk Mgmt)**: Challenge the generated hypotheses for flaws and extreme risk-taking.
- **QuantAgent**: Specialized in generating pure `vectorbt` backtesting scripts iteratively based on market conditions.
- **Trader**: Finalizes the discrete parameters of the asset to be bought or sold.

## System Topology
1. `graph/`: Orchestrates the flow of states between LLM nodes.
2. `strategy_lab/`: Handles sandboxing the code generated by `QuantAgent`, executing the `vectorbt` strategies, and promoting successful outputs to the `approved_scripts/` repository.
3. `execution/`: A resilient, deterministic engine that routes intents to live brokerages (Dhan / Delta) or the Local Paper Wallet. It manages Risk Gates and prevents duplicate executions.
4. `ops/`: Contains 24/7 lifecycle handlers like `daemon.py` which schedules operations, and `notifier.py` which alerts external messaging layers.

## The Life of a Trade
1. **Cron Trigger**: `daemon.py` wakes up the `TradingAgentsGraph`.
2. **Analysis**: Data flows from Providers -> Analysts -> Trader node.
3. **Intent Generation**: Yields a `TradeIntent`.
4. **Execution Gate**: Passes through `DeterministicRiskGate` and `ExecutionIdempotencyManager`.
5. **Broker Routing**: Executes on Dhan (Options), Delta (Crypto), or Paper (Simulation).
6. **Notification**: The event is journaled and sent via Webhook to Discord/Telegram.
## System Diagram (Mermaid)

```mermaid
graph TD
    %% Define Styles
    classDef llm fill:#f9f,stroke:#333,stroke-width:2px,color:#000;
    classDef system fill:#bbf,stroke:#333,stroke-width:1px,color:#000;
    classDef storage fill:#ffcc00,stroke:#333,stroke-width:1px,color:#000;
    classDef external fill:#ccffcc,stroke:#333,stroke-width:1px,color:#000;

    %% Data Sources
    subgraph Data Sources
        Dhan[Dhan APIs]:::external
        Delta[Delta Exchange]:::external
        YF[YFinance]:::external
    end

    %% Core Ops
    subgraph Core Operations ops/
        Daemon((daemon.py \n24/7 APScheduler)):::system
        Notifier[notifier.py \nDiscord/Telegram]:::system
    end

    %% Agent Graph
    subgraph Multi-Agent Market Graph graph/
        Researchers[Researchers]:::llm
        Analysts[Analysts]:::llm
        Debators[Risk Debators]:::llm
        TraderGraph[Trader Agent]:::llm
    end

    %% Strategy Lab
    subgraph Auto Quant Strategy Lab strategy_lab/
        QuantAgent[Quant Agent]:::llm
        Sandbox[Sandboxed VectorBT]:::system
        ApprovedScripts[(Approved Scripts)]:::storage
        DynRunner[Dynamic Runner]:::system
        KillSwitch[Portfolio Monitor]:::system
    end

    %% Execution Engine
    subgraph Execution Engine execution/
        Engine[Engine router.py]:::system
        RiskGate[Deterministic Risk Gate]:::system
        PaperWallet[(Local Paper Wallet)]:::storage
    end

    %% Workflows
    Daemon == "Hourly" ==> Researchers
    Researchers --> Analysts
    Analysts --> Debators
    Debators --> TraderGraph
    TraderGraph -. "Trade Intent" .-> Engine

    Daemon == "2AM Nightly" ==> QuantAgent
    QuantAgent --> |"Strategy Code"| Sandbox
    Sandbox --> |"Stack Trace/Low Sharpe"| QuantAgent
    Sandbox == "Sharpe > 1.0" ==> ApprovedScripts
    KillSwitch -- "If Trade PnL < -X" --> ApprovedScripts

    Daemon == "Every 15-Min" ==> KillSwitch
    Daemon == "Every 15-Min" ==> DynRunner
    DynRunner --> |"Loads & Runs"| ApprovedScripts
    Dhan --> |"Live Pricing"| DynRunner
    Delta --> |"Live Pricing"| DynRunner
    DynRunner == "Trade Intent" ==> Engine

    Engine --> RiskGate
    RiskGate -- "Approved" --> PaperWallet
    RiskGate -- "Options Buy/Sell" --> Dhan
    RiskGate -- "Crypto Buy/Sell" --> Delta
    
    %% Notifications
    RiskGate -. "Rejected Intent" .-> Notifier
    ApprovedScripts -. "Promoted Strategy" .-> Notifier
    PaperWallet -. "Trade Filled" .-> Notifier
    Dhan -. "Live Order Submitted" .-> Notifier
    Delta -. "Live Order Submitted" .-> Notifier

```
